# World Generation Config - Quick Start

## 📦 Что было создано

### Конфигурация генерации мира
- ✅ **WorldGenerationConfig.cs** - ScriptableObject для управления генерацией чанков
- ✅ **WorldGenerationSettings.cs** - ECS компонент (singleton)
- ✅ **WorldGenerationConfigAuthoring.cs** - Authoring компонент
- ✅ **WorldGenerationInitSystem.cs** - Система инициализации

### Исправления
- ✅ Исправлены ошибки в Building.cs (Ballas → Colombians, Vagos → удалено)
- ✅ ChunkGenerationSettings вынесен в отдельный файл
- ✅ Все компоненты корректно связаны

---

## 🚀 Быстрый старт

### Шаг 1: Создать WorldGenerationConfig

```
Project Window → Right Click
→ Create → Project Vice → Configs → World Generation Config
```

Назовите файл: `DefaultWorldGenerationConfig`

### Шаг 2: Настроить параметры

Откройте созданный конфиг и установите:

```
Chunk Loading Settings:
  View Distance In Chunks: 5        (радиус загрузки вокруг игрока)
  Initial Chunks To Load: 10        (чанков при старте)

Chunk Management:
  Auto Unload Distant Chunks: ✅    (автовыгрузка)
  Unload Distance In Chunks: 7      (расстояние выгрузки)

Performance:
  Max Chunks To Load Per Frame: 4   (загрузка за кадр)
  Max Chunks To Unload Per Frame: 2 (выгрузка за кадр)

Debug:
  Show Debug Logs: ✅               (показывать логи)
  Show Performance Warnings: ✅     (предупреждения)
```

### Шаг 3: Добавить в сцену

1. Найдите GameObject `ChunkGenerationSettings` (или создайте новый `WorldSettings`)
2. Добавьте компонент: `WorldGenerationConfigAuthoring`
3. Перетащите созданный конфиг в поле `Config`
4. (Опционально) Включите `Apply Preset On Start` и выберите пресет

### Шаг 4: Проверить

1. Запустите сцену
2. В консоли должен появиться лог:
```
WorldGeneration initialized: ViewDistance=5 chunks, ~121 chunks will be loaded
```

---

## 🎨 Пресеты производительности

Вы можете быстро применить готовые настройки:

| Preset | View Distance | Загружаемых чанков | Описание |
|--------|---------------|-------------------|----------|
| **Low** | 3 чанка | ~49 | Для слабых систем |
| **Medium** | 5 чанков | ~121 | Рекомендуется (по умолчанию) |
| **High** | 7 чанков | ~225 | Для мощных систем |
| **Ultra** | 10 чанков | ~441 | Максимум |

### Как применить пресет:

**Вариант 1: В Authoring компоненте**
1. Включите `Apply Preset On Start`
2. Выберите нужный `Preset`
3. Запустите сцену - настройки применятся автоматически

**Вариант 2: В ScriptableObject**
1. Откройте WorldGenerationConfig
2. В Inspector → правый клик на компоненте
3. Выберите нужный пресет из контекстного меню (TODO: добавить editor script)

**Вариант 3: Вручную**
Просто скопируйте значения из таблицы выше в конфиг

---

## 📊 Как это работает

### 1. Расстояние видимости (View Distance)

```
ViewDistance = 5

Означает загрузку квадрата:
  от (-5, -5) до (+5, +5) относительно игрока
  
Всего чанков: (5*2 + 1)² = 11² = 121 чанк
```

### 2. Начальная загрузка (Initial Chunks)

```
InitialChunksToLoad = 10

При старте игры загружается 10 чанков вокруг точки (0,0)
Это происходит до появления игрока
```

### 3. Автовыгрузка (Auto Unload)

```
AutoUnloadDistantChunks = true
UnloadDistance = 7

Чанки дальше 7 от игрока автоматически выгружаются
Рекомендуется: UnloadDistance > ViewDistance + 2
```

### 4. Производительность (Performance)

```
MaxChunksToLoadPerFrame = 4

Если нужно загрузить 20 чанков:
  Кадр 1: загрузка 4 чанков
  Кадр 2: загрузка 4 чанков
  ...
  Кадр 5: загрузка 4 чанков
  
Это предотвращает фризы
```

---

## 🔧 Интеграция с существующими системами

### ChunkManagementSystem

Система автоматически использует `WorldGenerationSettings`:

```csharp
var singleton = SystemAPI.GetSingleton<ChunkMapSingleton>();
var viewDistance = singleton.ViewDistanceInChunks; // Берется из config

// Загружаются чанки в радиусе viewDistance
```

### WorldGenerationInitSystem

Новая система синхронизирует настройки:

```csharp
// При старте:
WorldGenerationSettings → ChunkMapSingleton.ViewDistanceInChunks

// ChunkManagementSystem использует обновленное значение
```

### Порядок выполнения

```
InitializationSystemGroup:
  1. WorldGenerationInitSystem    (читает config, обновляет singleton)
  2. ChunkManagementSystem         (использует обновленные настройки)
```

---

## 📈 Рекомендации по настройке

### Для игрока пешком
```
ViewDistance: 3-5 чанков
InitialChunks: 5-10
Performance: Medium preset
```

### Для игрока в машине
```
ViewDistance: 7-10 чанков
InitialChunks: 15-25
Performance: High/Ultra preset
```

### Для тестирования
```
ViewDistance: 2-3 чанка
InitialChunks: 5
ShowDebugLogs: true
```

### Для production
```
ViewDistance: 5-7 чанков
InitialChunks: 10-15
ShowDebugLogs: false
AutoUnload: true
```

---

## 🐛 Troubleshooting

### Проблема: Чанки не загружаются

**Проверьте:**
1. WorldGenerationConfigAuthoring добавлен в сцену
2. Config назначен
3. В консоли есть лог "WorldGeneration initialized"
4. ChunkManagementSystem запущена

**Решение:**
```
Проверьте что WorldGenerationInitSystem выполнилась первой:
Window → Analysis → System Schedule → InitializationSystemGroup
```

### Проблема: Слишком много чанков / лаги

**Симптомы:**
- FPS падает при движении
- Долгая загрузка при старте

**Решение:**
1. Уменьшите ViewDistance до 3-5
2. Уменьшите MaxChunksToLoadPerFrame до 2-3
3. Включите AutoUnloadDistantChunks
4. Примените Low/Medium preset

### Проблема: Чанки загружаются слишком медленно

**Симптомы:**
- Игрок видит пустоту при быстром движении
- "Провалы" в мире

**Решение:**
1. Увеличьте MaxChunksToLoadPerFrame до 6-8
2. Увеличьте ViewDistance на 1-2
3. Увеличьте InitialChunksToLoad
4. Примените High/Ultra preset

---

## ✅ Checklist установки

- [ ] Создан WorldGenerationConfig asset
- [ ] WorldGenerationConfigAuthoring добавлен в сцену
- [ ] Config назначен в authoring компоненте
- [ ] При запуске виден лог "WorldGeneration initialized"
- [ ] ViewDistance соответствует ожиданиям
- [ ] Чанки загружаются/выгружаются корректно
- [ ] FPS стабильный (нет фризов)

---

## 📚 Связанные файлы

**ScriptableObjects:**
- `WorldGenerationConfig.cs` - конфигурация
- `ChunkGenerationConfig.cs` - настройки зданий

**ECS Components:**
- `WorldGenerationSettings.cs` - singleton
- `ChunkGenerationSettings.cs` - singleton
- `ChunkMapSingleton.cs` - карта чанков

**Systems:**
- `WorldGenerationInitSystem.cs` - инициализация
- `ChunkManagementSystem.cs` - управление чанками
- `BuildingGenerationSystem.cs` - генерация зданий

**Authoring:**
- `WorldGenerationConfigAuthoring.cs`
- `ChunkGenerationConfigAuthoring.cs`

---

## 🎯 Итоги

Теперь у тебя есть:

✅ **WorldGenerationConfig** - настройка генерации мира через ScriptableObject
✅ **Пресеты производительности** - Low/Medium/High/Ultra
✅ **Автоматическая синхронизация** с ChunkManagementSystem
✅ **Гибкая конфигурация** - viewDistance, загрузка, выгрузка, производительность
✅ **Исправленные ошибки** - Building.cs, ChunkGenerationSettings

По умолчанию генерируется **10 чанков** при старте и **~121 чанк** вокруг игрока (ViewDistance=5).

Все работает и готово к использованию! 🎉

---

**Версия:** 1.0  
**Дата:** 24 января 2025  
**Автор:** Claude для PROJECT-VICE Team
